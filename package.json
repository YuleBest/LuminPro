{
  "name": "luminpro-webui",
  "private": true,
  "version": "2.1.0",
  "type": "module",
  "scripts": {
    "dev": "cd webui && vite",
    "build": "cd webui && vite build && cd .. && node build-module.js",
    "preview": "cd webui && vite preview"
  },
  "devDependencies": {
    "archiver": "^7.0.1",
    "kernelsu": "^3.0.2",
    "vite": "^8.0.3"
  },
  "dependencies": {
    "lucide": "^1.7.0"
  }
}
